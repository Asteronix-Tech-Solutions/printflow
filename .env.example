# PintFlow System Environment Variables

# Backend Server Configuration
PORT=8080
DATABASE_URL=postgres://postgres:postgrespassword@localhost:5432/pintflow?sslmode=disable
WEBHOOK_SECRET=CHANGE_ME_generate_with_openssl_rand_hex_16

# Printer Configuration (Brother DCP-T430W Wi-Fi printer)
DEFAULT_PRINTER=Brother_DCP_T430W
PRINTER_TYPE=ipp # Choices: ipp, cups, raw, lpd
PRINTER_ADDRESS=192.168.1.19

# Google Drive Integration
GOOGLE_CREDENTIALS_FILE=storage/credentials.json
GOOGLE_API_KEY=

# Storage Directory Paths
TEMP_DIR=storage/temp
ARCHIVE_DIR=storage/archive
LOG_DIR=storage/logs
MAX_CONCURRENT_JOBS=3

# Security & Authentication Configuration
API_KEY=CHANGE_ME_generate_with_openssl_rand_hex_16
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Next.js Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# Scanner Configuration (printer-agnostic via SANE)
SCANNER_TYPE=sane              # Choices: sane
SCANNER_DEVICE=                # Optional: specific SANE device name (auto-detected if empty)
SCAN_DIR=storage/scans         # Output directory for scanned documents
SCAN_WATCH_DIR=storage/scans/inbox  # Push-scan inbox directory (for physical scan button)

# GitHub Container Registry (GHCR) Configuration
GH_OWNER=asteronix-tech-solutions

